# Native build env vars (Capacitor Android / iOS / Electron DMG).
# Applied when `vite build --mode native`.
# Vite only applies .env.production for mode=production, so we duplicate
# the public URL here so share links don't use the localhost origin.
VITE_APP_URL=https://botccompanion.web.app
